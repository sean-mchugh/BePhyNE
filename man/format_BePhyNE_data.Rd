% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Utils.R
\name{format_BePhyNE_data}
\alias{format_BePhyNE_data}
\title{Format presence/absence data for BePhyNE}
\usage{
format_BePhyNE_data(
  pa_data,
  tree,
  sp_col,
  occ_col,
  env_preds,
  scale_atr = NA,
  normalize_data = T
)
}
\arguments{
\item{pa_data}{A data frame containing species names, presence/absence values,
and environmental predictor columns.}

\item{tree}{A phylo object whose tip labels define the species order.}

\item{sp_col}{Name or index of the species column in `pa_data`.}

\item{occ_col}{Name or index of the presence/absence column in `pa_data`;
values should be coded as 0/1.}

\item{env_preds}{Character vector giving the environmental predictor columns
to include.}

\item{scale_atr}{Optional list with `center` and `scale` entries, usually
returned by a previous call to this function. If `NA`, scaling parameters
are estimated from `pa_data`.}

\item{normalize_data}{Logical; if `TRUE`, center and scale the environmental
predictors.}
}
\value{
A list with elements `data`, the species-ordered BePhyNE data object,
  and `scale`, the scaling attributes used for predictors.
}
\description{
Converts a presence/absence data frame into the list-of-lists format used by
BePhyNE, orders species to match the supplied phylogeny, optionally scales
environmental predictors, and stores the scaling attributes needed for later
back-transformation.
}

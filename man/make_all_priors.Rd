% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Priors_fns.R
\name{make_all_priors}
\alias{make_all_priors}
\title{Construct default BePhyNE prior objects}
\usage{
make_all_priors(
  N,
  tips,
  bd_range = NA,
  root_opt_mean = rep(0, N),
  root_opt_sd = rep(0.2, N),
  root_brdth_meanlog = rep(log(0.3), N),
  root_brdth_sdlog = rep(0.1, N),
  sigsq_opt_meanlog = rep(log(0.1), N),
  sigsq_opt_sdlog = rep(0.5, N),
  sigsq_brdth_meanlog = rep(log(0.1), N),
  sigsq_brdth_sdlog = rep(0.5, N),
  heights_mean_by_sp = sample(0.95, size = tips, replace = TRUE),
  heights_sd_by_sp = sample(0.15, size = tips, replace = TRUE),
  use_glm_height_mean = F,
  species_data = NA,
  tree = NA,
  miss_data_uninf = T,
  uninf_height_mean = 0.5,
  uninf_height_sd = 1.5,
  r = 2,
  p = 1,
  plot = TRUE
)
}
\arguments{
\item{N}{Number of environmental predictors.}

\item{tips}{Number of species/tips in the phylogeny.}

\item{bd_range}{Optional breadth range object; currently forwarded internally
when supplied.}

\item{root_opt_mean, root_opt_sd}{Mean and standard deviation for the root
optimum prior, one value per predictor.}

\item{root_brdth_meanlog, root_brdth_sdlog}{Meanlog and sdlog for the root
breadth prior, one value per predictor.}

\item{sigsq_opt_meanlog, sigsq_opt_sdlog}{Meanlog and sdlog for the optimum
evolutionary variance prior.}

\item{sigsq_brdth_meanlog, sigsq_brdth_sdlog}{Meanlog and sdlog for the breadth
evolutionary variance prior.}

\item{heights_mean_by_sp}{Prior mean for the species-level tolerance/height
parameter.}

\item{heights_sd_by_sp}{Prior standard deviation for the species-level
tolerance/height parameter.}

\item{use_glm_height_mean}{Logical; if `TRUE`, estimate height priors from GLM
starting values using `species_data` and `tree`.}

\item{species_data}{Optional BePhyNE data list used when estimating GLM-based
height means.}

\item{tree}{Optional phylo object used when estimating GLM-based height means.}

\item{miss_data_uninf}{Logical; if `TRUE`, use a broader height prior for
species with missing data.}

\item{uninf_height_mean, uninf_height_sd}{Mean and standard deviation for the
broader missing-data height prior.}

\item{r}{Number of niche traits in the multivariate Brownian-motion prior.}

\item{p}{Number of rate regimes.}

\item{plot}{Logical; if `TRUE`, plot prior densities where implemented.}
}
\value{
A list of BePhyNE prior objects, one per predictor.
}
\description{
Builds one prior object per environmental predictor for the root niche
parameters, evolutionary variance parameters, correlations, and species-level
tolerance/height terms. This is the high-level prior constructor used in the
vignette.
}

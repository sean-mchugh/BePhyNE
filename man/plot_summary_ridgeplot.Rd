% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plotting_fns.R
\name{plot_summary_ridgeplot}
\alias{plot_summary_ridgeplot}
\title{Plot Species Response Curves from One or More Summary Objects}
\usage{
plot_summary_ridgeplot(
  tree,
  log_summary,
  model_names = NULL,
  predictor_names = NA,
  scale_atr = NA,
  curve_colors = NULL,
  curve_fill_colors = NULL,
  pt_colors = NULL,
  line_types = NULL,
  xlims = NULL,
  predictor_name_cex = 0.4,
  xlabel_cex = 0.4,
  tip_cex = 0.3
)
}
\arguments{
\item{tree}{A phylogenetic tree of class `"phylo"`.}

\item{log_summary}{Either a single summary object produced by [summarize_logdf()], or a list of such summary objects.}

\item{model_names}{Character vector of model names used in the plot legend.
If `NULL`, names are generated automatically as `"model1"`,
`"model2"`, etc.}

\item{predictor_names}{Character vector giving predictor names for each
environmental variable. If `NA`, generic names (`"Predictor 1"`,
`"Predictor 2"`, etc.) are used.}

\item{scale_atr}{Optional scaling information used to back-transform
predictor values. Typically a list containing elements `center`
and `scale`. Defaults to `NA`.}

\item{curve_colors}{Character vector of line colors for each model.}

\item{curve_fill_colors}{Character vector of fill colors for each model.}

\item{pt_colors}{Character vector of point colors for each model.}

\item{line_types}{Integer or character vector specifying line types for
each model.}

\item{xlims}{Optional list of x-axis limits, one element per predictor.
If `NULL`, limits are determined automatically.}

\item{predictor_name_cex}{Numeric scaling factor for predictor labels.}

\item{xlabel_cex}{Numeric scaling factor for x-axis labels.}

\item{tip_cex}{Numeric scaling factor for phylogeny tip labels.}
}
\value{
Invisibly returns the output of [plot_response_curves()].
}
\description{
Generates ridge-style response curve plots from one or more BePhyNE
summary objects. If a single summary object is supplied, a single model
is plotted. If a list of summary objects is supplied, response curves are
overlaid for comparison among models.
}
\details{
The function extracts median species-level trait estimates from
`median_parlist$traits` and passes them to
[plot_response_curves()] for visualization.


For each model, the function extracts median trait estimates from
`median_parlist$traits`. Each predictor is expected to contain a matrix
of species-specific niche parameters (e.g., optimum, breadth, tolerance).

Multiple models are plotted simultaneously by overlaying response curves
for direct visual comparison.
}
\examples{
\dontrun{

# Single model
plot_summary_ridgeplot(
  tree = tree,
  log_summary = full_log_summary
)

# Multiple models
plot_summary_ridgeplot(
  tree = tree,
  log_summary = list(
    full_log_summary,
    miss_log_summary
  ),
  model_names = c(
    "With data",
    "Missing data"
  )
)

}

}
\seealso{
[plot_response_curves()]
}
